# Deteccion de sarcasmo

Este proyecto usa `uv` para reproducir el entorno de Python.

## Preparar el entorno

```bash
uv sync
```

## Usar notebooks

```bash
uv run jupyter lab
```

El modelo de spaCy `es_core_news_sm` esta declarado como dependencia del proyecto, asi que se instala con `uv sync`.
